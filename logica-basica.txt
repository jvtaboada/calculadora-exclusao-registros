CONSIDERANDO INTERVALO ENTRE DOIS SELECT COUNT REGISTROS
TEMPO/INTERVALO:
hora e min iniciou? (formato 24h)
VARIAVEL INT horaInicio / minInicio

hora e min fim? (formato 24h)
VARIAVEL INT horaFinal / minFinal

tempo intervalo (em min)
VARIAVEL INT tempoIntervalo = horaFinal - horaInicio & minFinal - minInicio = x mins


QTD REGISTROS EXCLUIDOS NO INTERVALO:
quantos registros inicio?
VARIAVEL INT regInicio

quantos registros fim?
VARIAVEL INT regFinal

VARIAVEL INT regIntervalo = regInicio - regFinal 


CÁLCULO VELOCIDADE (reg/s):

VARIAVEL INT regDelMin = regIntervalo (101.788) / tempoIntervalo (38 min) ;

VARIAVEL INT regDelSeg = regDelMin / 60 ;


X = REG INICIO - REG FIM  

Y= TEMPO FIM - TEMPO INICIO

(X/Y)60 = valor em reg/s




